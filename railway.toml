[build]
builder = "nixpacks"

[environment]
PYTHONUNBUFFERED = "true"
DATABASE_URL = "${{Neon.DATABASE_URL}}"
REDIS_URL = "${{Redis.REDIS_URL}}"

[start]
cmd = "cd backend && gunicorn main:app --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT --timeout 120"
