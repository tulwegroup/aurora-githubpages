# Force nixpacks to recognize both Python and Node.js
[providers]
# Explicitly enable Python provider to detect requirements.txt
python.enabled = true
# Explicitly enable Node.js provider
nodejs.enabled = true

[phases.install]
# Install Python dependencies before building frontend
dependsOn = ["setup"]
commands = ["pip install --no-cache-dir -r requirements.txt"]
