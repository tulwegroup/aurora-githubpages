[phases.install]
# Install Python dependencies at BUILD time using python3 -m pip
dependsOn = ["setup"]
cmds = [
  "python3 -m pip install --no-cache-dir -r requirements.txt"
]

[phases.build]
# Build frontend
dependsOn = ["install"]
cmds = [
  "npm run build"
]
